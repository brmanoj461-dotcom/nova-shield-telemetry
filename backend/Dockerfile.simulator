FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

# Explicitly install requests alongside requirements
COPY requirements.txt .
RUN pip install --no-cache-dir requests fastapi uvicorn pydantic -r requirements.txt

COPY . .

CMD ["python", "simulator.py"]